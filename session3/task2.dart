void main() {

  //  two sets
  // intersection
  // union
 Set<int> setNumbers1 = {
    1 ,
    2,
    3
  };
  Set<int> setNumbers2 = {
    1 ,
    3,
    4,
    5
  };
// print(  setNumbers1.intersection(setNumbers2));
//
// print(setNumbers1.union(setNumbers2));

print(setNumbers2.difference(setNumbers1));








  // Define two sets of integers
  // Set<int> set1 = {1, 2, 3, 4};
  // Set<int> set2 = {3, 4, 5, 6};
  //
  // // Calculate the intersection of the sets
  // Set<int> intersection = set1.intersection(set2);
  //
  // // Calculate the union of the sets
  // Set<int> union = set1.union(set2);
  //
  // // Print out the intersection and union
  // print("Intersection: $intersection");
  // print("Union: $union");
}
