import 'dart:io';

main(  )  {

  /*
  * Create a map that maps strings to booleans
  *  and use the containsKey
  *  method to check if a specific key is in the map.
  *  */







  //
  //
  //
  //
  // if (map.containsKey(word)) {
  //   print('The map contains the key "$word"');
  // } else {
  //   print('The map does not contain the key "$word"');
  // }

  // print ("enter a size of map: ");
  // int size = int.parse(stdin.readLineSync()!);
  // Map<String , bool>  map = {} ;
  // for( int i = 0 ;i<size;i++ )  {
  //   print ("enter a string ${i+1}");
  //   String word = stdin.readLineSync()!;
  //   print ("enter a bool ${i+1}");
  //   String bl = stdin.readLineSync()!;
  //
  //   map.addAll({word : bl.toLowerCase()=='true'});
  //
  // }

}